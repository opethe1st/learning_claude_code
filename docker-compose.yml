services:
  db:
    image: postgres:16-alpine
    container_name: learning_claude_code_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: learning_claude_code
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: learning_claude_code_app
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/learning_claude_code
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./src:/app/src
    command: uvicorn learning_claude_code.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
